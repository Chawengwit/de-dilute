# VCS
.git
.gitignore

# Logs / junk
*.log
npm-debug.log
yarn-error.log
pnpm-debug.log
.DS_Store
Thumbs.db

# Envs (ใช้ ENV ของ Render แทน)
.env
.env.*
!.env.example

# Node deps (ทุกรายการย่อย)
node_modules
**/node_modules

# Build outputs (ถ้ามี)
dist
build
coverage

# ไม่ต้องส่งของ backend/database เข้า build context ของ Nginx
backend
database
docker-compose.yml

# Frontend source maps/SCSS ที่ไม่จำเป็นบน production
frontend/**/*.map
frontend/scss
